base_view: omni__grouping_total_sales_volume
label: New label topic

default_filters:
  terms_groups:
    is: Tops

joins: {}

views:
  omni__grouping_total_sales_volume:
    measures:
      newest_field:
        sql: COUNT(DISTINCT ${omni__grouping_total_sales_volume.terms})
        label: Newest field
